
OBJETO DATES: 

Crear una funcion global de Date, a partir de la cual saldran las fechas del programa.




LOGIN:

1: Pedir datos al usuario.

2: Validar datos del usuario:

2.1 Validar nombre de usuario:
	-Buscar el nombre de usuario en la lista de USUARIOS.
	-SI aparece en la lista, pasar a validacion de contraseña.
	-SI NO aparece en la lista, alert con error ("no se ha encontrado el nombre de usuario"). Pedir al usuario que ingrese bien su nombre o que se registre.

2.2 Validar contraseña:
	-Buscar contraseña ingresada en las propiedades de lista USUARIOS.
	-SI aparece en la lista, ingresar al .index.
	-SI NO aparece en la lista, alert con error ("contraseña incorrecta"). Pedir al usuario que ingrese bien la contraseña.

3: Utilizar los datos rellenados para crear el CURRENT_USER.

4: Enviar al usuario al index.





SIGN IN:

1:Pedir al usuario que rellene el formulario.

2: Extraer datos (values) de los inputs rellenados por el usuario a variables.

2.1 Con estos values/variables crear nuevo usuario de la clase USER y agregar a la lista USUARIOS.

2.2 Exportar datos de la lista USUARIOS en formato JSON para guardar en storage.

3: De los datos rellenados, extraer los necesarios para crear el CURRENT_USER.

4: Enviar al usuario al index.




INDEX: 

1: En barra nav superior, mostrar el usuario activo CURRENT_USER.

2: Mostrar proyectos en el board con data extraida de PROJECT_LIST array (deberia estar almacenada en formato JSON).




NEW PROJECT: 

1: Pedir al usuario que rellene los datos necesarios.